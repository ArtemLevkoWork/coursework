# 🏖️ Туристическое агентство - Coursework

Веб-приложение для туристического агентства с системой администраторов и клиентов.

## 🚀 Быстрый старт

### 1. Настройка базы данных

```bash
# Создание таблиц (если нужно)
node setup-db.js

# Заполнение тестовыми данными
node seed-db.js
```

### 2. Запуск приложения

```bash
node app.js
```

Приложение будет доступно по адресу: http://localhost:3000

## 👥 Тестовые учетные записи

### Администратор
- **Email:** `admin@voyariestuff.com`
- **Пароль:** `admin123`

### Клиент
- **Email:** `client@example.com`
- **Пароль:** `client123`

## 🔧 API Endpoints

### Аутентификация
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход из системы
- `GET /api/check-auth` - Проверка авторизации
- `POST /api/register-client` - Регистрация клиента
- `POST /api/register-admin` - Регистрация администратора

### Туры
- `GET /api/tours` - Получение списка туров
- `POST /api/tours` - Создание тура (только админ)
- `PATCH /api/tours/:id` - Обновление тура (только админ)
- `DELETE /api/tours/:id` - Удаление тура (только админ)
- `GET /api/tours/:id/requests` - Запросы на тур (только админ)
- `POST /api/tours/:id/request` - Оставить заявку на тур
- `GET /api/tours/:id/reviews` - Отзывы о туре
- `POST /api/tours/:id/reviews` - Оставить отзыв

### Админ-панель
- `GET /api/admin/requests` - Все запросы (только админ)
- `PATCH /api/admin/requests/:id` - Изменить статус запроса (только админ)

## 📁 Структура проекта

```
├── app.js                 # Главный файл сервера
├── package.json          # Зависимости Node.js
├── .env                  # Конфигурация (не коммитить!)
├── create-tables.sql     # SQL скрипт создания таблиц
├── setup-db.js          # Скрипт настройки БД
├── seed-data.js         # Скрипт заполнения тестовыми данными
├── test-db.js           # Скрипт тестирования подключения к БД
└── public/              # Статические файлы
    ├── *.html           # HTML страницы
    ├── *.css            # Стили
    ├── *.js             # Клиентский JavaScript
    └── images/          # Изображения
        ├── footerDec.png
        ├── heroFTImage.jpg
        ├── heroFTImage2.jpg
        ├── heroFTImage3.jpg
        ├── logInBg.jpg
        ├── paris.jpg     # Изображение тура "Париж"
        ├── bali.jpg      # Изображение тура "Бали"
        ├── alps.jpg      # Изображение тура "Альпы"
        └── safari.jpg    # Изображение тура "Сафари"
```

## 🗄️ База данных

### Таблицы
- `admins` - Администраторы
- `clients` - Клиенты
- `tours` - Туры
- `toursrequests` - Заявки на туры
- `toursreviews` - Отзывы о турах

### Конфигурация
Настройки подключения к БД находятся в файле `.env`:

```env
DB_HOST=localhost
DB_USER=root
DB_PASS=1234
DB_NAME=tours_db
JWT_SECRET=very_strong_secret_here
FRONTEND_ORIGIN=http://localhost:5500
NODE_ENV=development
```

## 🔒 Аутентификация

- **Логин работает без указания имени** - достаточно email и пароль
- JWT токены используются для авторизации
- Разделение ролей: `admin` и `client`
- Cookies используются для хранения токенов

## 🖼️ Изображения

Изображения хранятся в папке `public/images/`:
- Статические изображения UI
- Изображения туров (ссылаются из поля `toursCover` в БД)

**ВНИМАНИЕ:** Изображения туров (`paris.jpg`, `bali.jpg`, `alps.jpg`, `safari.jpg`) нужно добавить вручную!

## 🛠️ Разработка

### Установка зависимостей
```bash
npm install
```

### Тестирование подключения к БД
```bash
node test-db.js
```

### Добавление тестовых данных
```bash
node seed-data.js
```

## 📝 Примечания

- Убедитесь, что MySQL сервер запущен
- Проверьте настройки в `.env` файле
- Для продакшена измените `JWT_SECRET` и настройки безопасности
